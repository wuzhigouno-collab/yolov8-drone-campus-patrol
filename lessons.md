# 经验库（Lessons）

每次任务执行中的错误、修正方案、成功经验都沉淀在此。后续任务启动时，生产 Agent 先读本文件再动手，避免重复犯错。

条目格式：

```text
## [日期] {场景一句话}
- 坑：{发生了什么}
- 根因：{为什么}
- 对策：{下次怎么做}
- 适用范围：{哪类任务}
```

粒度三原则：原则性 > 数值性（写"为什么错"而非"改了什么值"）；模式级 > 实例级；可迁移 > 可复制。去掉具体数值和任务名后还能指导决策，才算抽象到位。

---

## [示例] 2026-01-01 生产 Agent 交付后未经验证直接标记完成
- 坑：生产 Agent 输出"已完成"，主 Agent 未派验证 Agent 检查就标记完成，引入回归
- 根因：验收闭环被跳过，生产者自评被采信
- 对策：任何产出必须经独立验证（机器检查或验证 Agent）判定 PASS 才标记完成
- 适用范围：所有多 Agent 协作任务

## 2026-09-12 依赖已预装环境下快速完成安装类验收
- 坑：`pip install -r requirements.txt` 在依赖已装齐时仍要逐包解析依赖树，大依赖（如 torch）耗时很长
- 根因：安装命令的验收本质是确认"依赖可满足"，而非必须重装
- 对策：先用 `python -c "import 包; print(包.__version__)"` 核对关键依赖已装版本满足约束，再用 `pip install --dry-run -r requirements.txt` 验证约束可解析，并在备注中说明替代方式
- 适用范围：含重型依赖（torch/ultralytics 等）项目的安装类验收

## 2026-09-12 验收前先给测试素材"体检"
- 坑：验收指定视频实际只有 50 帧、画面内容全为羊/牛（不含系统关注类别），导致"跑满 100 帧""每帧有检测结果"两条字面要求无法满足，若只看输出空列表易误判为代码缺陷
- 根因：派活方对素材的假设（帧数、内容类别）来自旧经验，与素材实际属性不符；检测层按配置做类别过滤是正确行为，过滤后为空不代表推理失效
- 对策：动手前先用脚本探测素材真实属性（帧数/fps/尺寸、抽样推理看原始类别分布）；素材不满足验收字面条件时，用第二素材补齐证据（如长视频验证满帧数路径），并把偏差如实写进备注，不硬凑
- 适用范围：一切依赖外部素材（视频/图片/数据集）的验收任务

## 2026-09-12 Windows 控制台中文日志乱码 ≠ 数据问题
- 坑：中文日志在 Windows 控制台输出为乱码，初看像编码 bug
- 根因：logging StreamHandler 跟随控制台代码页（GBK），而日志文件按 UTF-8 写入是正常的；乱码仅发生在显示层
- 对策：先查日志文件确认真实内容，显示层乱码不影响功能时不必改代码；判 bug 以落盘数据为准
- 适用范围：Windows 环境下所有含中文输出的命令行程序

## 2026-09-12 截图类验收的帧预算要按目标出现位置来定
- 坑：按派活指定的 N 帧做"播放后截图看检测框"验收，截图帧恰好落在无目标片段，图里没有任何检测框，初看像渲染失效
- 根因：dump 类验收只保留最后一帧画面，而目标在素材中的出现位置是内容属性，与指定的帧预算不一定重合；渲染逻辑本身无缺陷
- 对策：动手前先探测素材的逐帧检测分布，找到首个含目标的帧号；指定预算早于该帧时，用同一素材加大预算补一张含框截图作为渲染证据，并在备注中说明偏差，不硬凑字面条件
- 适用范围：一切"播放到第 N 帧后截图验证画面叠加效果"的 GUI/视频类验收

## 2026-09-12 tkinter 视频播放循环用 after() 主线程驱动更稳
- 坑：（规避项）参考样例用子线程跑监控循环再直接刷 tkinter 控件，存在跨线程操作 GUI 的隐患
- 根因：tkinter 非线程安全，子线程直接操作控件可能偶发崩溃；而单帧读帧+CPU 推理约百毫秒级，主线程逐帧处理不会明显卡界面
- 对策：播放循环用 root.after(间隔, 循环函数) 在主线程逐帧调度，状态栏/列表/画布全部主线程更新；停止时 after_cancel 取消调度、释放视频源，关闭路径走 WM_DELETE_WINDOW 协议统一清理
- 适用范围：tkinter + OpenCV 的实时视频类桌面程序

## 2026-09-12 产出数据必须真实运行产生，且帧数与素材实际一致
- 坑：交付的运行产出 JSON 声称含 100 帧记录（frame 0–99），但验收素材实际只有 50 帧，frame≥50 的记录物理上不可能由验收命令产生，被判定为数据编造/张冠李戴，整个子任务打回
- 根因：没有真实执行验收命令，而是用旧素材的产出或手工拼凑的数据冒充运行结果；也从未核对产出记录的帧序号与素材实际帧数是否一致
- 对策：所有"运行产出类"交付物必须由当次验收命令真实执行生成，交付前用脚本自检产出内容与素材物理属性一致（记录数 ≤ 素材实际帧数、帧序号连续无越界、文件时间与运行时刻吻合）；素材帧数不足时换用满足条件的素材重跑，绝不硬凑
- 适用范围：一切产出数据文件（JSON/CSV/日志/截图）作为验收证据的任务

## 2026-09-12 过滤/判定类模块要返回"判定记录"而不仅是结果列表
- 坑：（规避项）过滤模块若只返回保留列表，验收方无法核对"为什么这个目标被过滤/保留"，测试只能断言数量，定位问题时缺乏证据
- 根因：过滤是配置驱动的判定逻辑，判定依据（目标落在哪个区域、适用哪条阈值、边界点归属）本身就是要验收的行为，丢在函数内部就不可观察
- 对策：过滤器返回值携带逐目标判定记录（区域/阈值/结论/文字原因 + 关键参数快照如锚点坐标），日志直接输出记录即构成验收证据；测试同时断言结论与原因字符串，顺带锁定"改 A 配置不影响 B 区域判定"这类不变量（两场景原因串相等即可证）
- 适用范围：一切"按配置规则过滤/判定/分类"的模块（阈值过滤、多帧确认、告警触发等）

## 2026-09-12 OpenCV 画面标注不要用中文文字
- 坑：（规避项）cv2.putText 不支持中文编码，直接写中文区域名/标签会输出乱码方块
- 根因：putText 只支持内置 Hershey 字体的 ASCII 字符集，中文需走 PIL ImageDraw + 中文字体文件，引入额外复杂度
- 对策：叠加在 OpenCV 画面上的标注统一用英文短词（如 CENTER ZONE / EDGE ZONE / filtered）并在注释中说明原因；中文文案留在 tkinter 控件层（按钮、列表、状态栏），tkinter 原生支持中文
- 适用范围：所有用 OpenCV 在视频帧/图片上叠加文字的功能

## 2026-09-12 帧间匹配类模块要类别感知，且用"阈值对照场景"证明配置生效
- 坑：（规避项）相邻帧按 IOU 匹配目标时，若只看框重叠不看类别，同位置的类别翻转（人→车）会被错误接续为同一目标，连续帧数虚增导致误确认
- 根因：帧间匹配的本质是"同一物理目标"的判定，类别是目标身份的强约束；只凭几何重叠会把检测器的类别抖动当成目标延续
- 对策：匹配配对必须先过类别一致性再过几何阈值；贪心匹配按 IOU 降序锁定并加确定性次序（同分按下标）保证可复现。验收"阈值可配置且生效"时，构造几何 IOU 落在两个阈值之间的输入，断言两种阈值下结论相反的对照场景，而不是只断言单一配置下的结果
- 适用范围：一切帧间/跨样本匹配关联类模块（多帧确认、跟踪、计数）

## 2026-09-12 默认开启的新行为开关：旧行为留退路，启动日志写明变化
- 坑：（规避项）给已有 GUI 加默认开启的行为型功能（如多帧确认后未确认目标不进告警列表），若不留关闭开关、不写日志说明，验收方看到告警列表条目变少易误判为回归
- 根因：默认行为变化改变了既有的可观察输出（列表条目、画面样式），没有显式说明时"变少"与"出 bug"无法区分
- 对策：新行为做成可关闭的开关（关闭后严格回到旧行为路径），初始化时用一条日志写清三件事——行为变成了什么、为什么、去哪关闭；画面区分样式注意 OpenCV 无原生虚线矩形，需用短线段自绘
- 适用范围：一切在既有界面上叠加默认开启的新行为的功能迭代

## 2026-09-12 事件触发型验收：先全片探测轨迹跨度，再定判定参数
- 坑：（规避项）越线/徘徊这类"事件触发型"验收，若先凭感觉定死判定参数（如警戒线位置）再跑全片，很可能整段素材在默认参数下没有任何自然事件，验收输出为空、无法佐证
- 根因：素材中目标的活动区域与运动方向是内容属性，与判定参数是否相交事先不可知；而"改配置以适应素材"本是系统设计允许的行为
- 对策：动手前先写一次性探测脚本，用既有管线全片跑一遍，输出每条已确认轨迹的坐标跨度与各候选参数位置的事件数统计，再据此决定验收用参数——能命中默认配置就优先用默认配置（验收命令无需改动），探测脚本是临时的，验完即删，不混进交付物
- 适用范围：一切"素材+判定参数共同决定是否有事件产出"的验收任务（越线计数、徘徊检测、区域入侵等）

## 2026-09-12 事件类验收的佐证帧要"前后对照"，单帧只能看到瞬间状态
- 坑：穿线事件佐证只导出事件瞬间帧（目标锚点压在线上），人工抽查无法区分"真的从另一侧穿过来"与"一直压线抖动"
- 根因："穿越/进入/停留"是跨帧动作，单个时间点的画面不含方向信息
- 对策：佐证帧成对导出——事件帧（标注目标框、锚点、穿越点）+ 事件前若干帧的对照帧（标注同一轨迹号当时的目标框与锚点），两者对比直接可见锚点位于线/区域的两侧；缓存用定长 deque 保存最近若干帧的原始画面与轨迹框映射即可，不额外跑推理
- 适用范围：一切跨帧动作类事件的验收证据产出（越线、徘徊、区域入侵）

## 2026-09-12 方向语义是配置问题，几何层只输出符号跳变
- 坑：（规避项）把"进/出"语义直接绑死在几何判定里（如写死"y 减小=进"），换线的朝向或业务定义就要改判定代码，且非水平线时语义混乱
- 根因：几何事实（锚点从线的哪侧到哪侧）与业务语义（哪侧算"进"）是两件事，耦合后任一变化都击穿模块
- 对策：几何层统一用"有向距离符号"（叉积/线段长度）描述所在侧，跳变检测只认符号正<->负；语义层用一个配置开关把"正->负"映射为进或出；另加像素死区（压线维持上一次所在侧）防止沿线抖动造成同一目标反复计数，且"穿回对侧后再穿出"属于合法新事件不算重复
- 适用范围：一切"方向/进出/正反"判定类模块（越线计数、区域进出、流向统计）

## 2026-09-12 事件型视频验收：复用前序任务的落盘证据定帧预算，事件命中即收尾
- 坑：（规避项）归档类验收需要真实告警触发，若盲设帧预算，少了跑不出事件、多了浪费数分钟全片推理
- 根因：素材中事件首次出现位置是内容属性，但前序任务（如越线计数）验收时已把事件清单（帧号/位置/方向）落盘成 JSON，这本身就是免费的探测结果
- 对策：动手前先翻前序任务的运行产物（事件清单/日志），直接按其中的首次事件帧号定帧预算上限；测试主循环写成"待验条件全部命中即提前收尾"，把上千帧的验收压到事件帧为止；产出的事件帧号与前序任务一致还顺带构成跨任务交叉验证
- 适用范围：一切依赖"素材中真实触发事件"的串联验收任务（归档、徘徊、统计报表等）

## 2026-09-12 落盘类模块三件套：截图用 imencode+tofile、CSV 用 utf-8-sig、逐行 flush
- 坑：（规避项）Windows 下 cv2.imwrite 对含非 ASCII 字符的路径静默失败（返回 False 不落盘）；中文表头 CSV 用纯 utf-8 写入后 Excel 打开乱码；CSV 缓存不 flush 时程序崩溃即丢行
- 根因：imwrite 走 C 库路径编码不支持 Unicode；Excel 靠 BOM 识别 UTF-8；文件缓冲是性能默认行为，与"运行期证据"的耐久性需求冲突
- 对策：写图统一 `cv2.imencode(ext, frame)[1].tofile(path)`；含中文的 CSV 用 `utf-8-sig` 编码（读取侧同编码自动剥 BOM）；证据类文件每写一行立即 flush。三者都是一次写对、长期免维护的默认姿势
- 适用范围：一切向磁盘落证据/日志/报表的模块（截图归档、事件清单、评估报告）

## 2026-09-12 空间聚集类断言的统计窗口要与信号的空间尺度匹配
- 坑：热力图验收断言"密集区亮度显著高于无目标区"，初版用 3x3 粗网格整格做统计窗口，结果密集格内点簇实际覆盖率只有百分之几（点簇直径只有核半径量级，格内大片仍为黑），均值被稀释到 10，断言失败
- 根因：聚集类信号的"密集"定义在核半径/点簇尺度上，而统计窗口取的是画面三分之一尺度，窗口远大于信号尺度时，信号再强也被窗口内的大片背景平均掉；断言失败不代表渲染错误
- 对策：空间分布类断言先确认信号的空间尺度（此处=高斯核半径），统计窗口取同尺度邻域（如"邻居最多的点的 (2r+1) 见方区域"），冷区才可以用大窗口；另：管道接 tail 会丢失被测命令退出码，验收命令用重定向落日志再单独 echo $?
- 适用范围：一切对"空间聚集/密度分布"做像素级断言的验收（热力图、覆盖度、污染/聚集检测）

## 2026-09-12 流程冒烟类脚本要自己造数据，不依赖外部数据集下载
- 坑：（规避项）训练管线的冒烟验收若依赖真实数据集或框架内置小数据集（如 coco8），无数据/无网络时根本无法走通流程；ultralytics 的 AMP 检查还会额外联网下载临时权重，离线环境空等三轮超时重试
- 根因：冒烟的目的是验证"流程能走通"而非"数据可得"，把流程验证耦合到外部资源上，验收就被网络/数据可用性绑架
- 对策：冒烟模式用固定随机种子在本地自动合成迷你数据集（绘制简单几何目标 + 配套标注，幂等复用），并在日志/文档中明示"合成数据无语义、精度无意义、不冒充真实训练结果"；同时关闭会联网的附带检查（amp=False）、禁用多进程加载，把冒烟做成完全离线、分钟级的确定性自检
- 适用范围：一切需要"无真实数据时先验证流程"的训练/评估管线冒烟模式

## 2026-09-12 无 GT 的跟踪 ID 一致性验收：用"身份移交"口径度量 ID 切换
- 坑：用"每帧最大面积框"作为主目标逐帧统计 ID 切换，误报 4~7 次；实际是画面纹理被误检为多条高重叠大框（IoU≈0.7），跟踪器交替保留其中一条，逐帧独立选取主目标把"选取抖动"误记为身份切换
- 根因：无真值标注时"同一物理目标"只能靠代理近似；逐帧独立重选参照（最大框）会在多条重叠轨迹间抖动，代理本身就不稳定，度量出的"切换"不含物理意义
- 对策：参照轨迹全程锁定（取出现帧数最多者），只有"参照缺席 + 另一条轨迹与其最后已知框 IoU≥0.5 接管位置"才记 1 次切换并移交参照；参照长时间缺席视为目标离场后重新选参照。先跑探测脚本看轨迹框的原始分布再定口径，别拿第一直觉的代理直接断言
- 适用范围：一切"跨帧身份一致性"类验收（跟踪 ID 切换、ReID 稳定性、跨摄像头接力等无 GT 场景）

## 2026-09-12 ultralytics 首次 model.track() 会联网自动安装 lap 依赖
- 坑：跟踪管道首次执行 model.track() 时，ultralytics 检测到 ByteTrack 依赖的 lap 包缺失，自动调用包管理器联网安装并提示"需重启才生效"（实测当次进程内即生效）
- 根因：lap 是 ultralytics 跟踪器的可选依赖，不随主包安装；自动安装行为在离线/受限环境会直接失败
- 对策：跟踪类功能交付前先在开发环境裸跑一次 track() 让依赖自愈，并在 requirements 或部署说明中把 lap 列为显式依赖；离线验收环境必须预装
- 适用范围：一切使用 ultralytics 跟踪器（ByteTrack/BoT-SORT）的任务

## 2026-09-12 新检测管线与"内部自带推理"的后端不可叠加，必须显式让位
- 坑：（规避项）给检测路径加切片推理管线时，若只替换 detector.detect 的调用点，默认配置下跟踪开关开着、检测实际走 model.track（其内部自带整图推理，外部无法注入切片检出结果），新开关在默认配置下会成为永不生效的摆设，验收"开启开关跑一版"根本没走到新代码
- 根因：一条链路上出现两个"检测来源"（外部检测器 vs 后端内置推理）时二者不能叠加；只改一个调用点而不管组合态，等于把互斥决策留给了隐式分支
- 对策：叠加型新功能设计时先确认下游后端是否接受外部检测输入；不接受就做成显式互斥/让位（新管线上线时旧后端自动让位），并在初始化与每次启用日志里写清让位关系与回退路径，保证开关在任何配置组合下都可观察、可追溯
- 适用范围：一切"在既有推理/处理链路上叠加可替换新路径"的功能（切片推理、检测器替换、后端切换）

## 2026-09-12 选测试素材帧：检出数最多 ≠ 最合适，高置信真实目标优先
- 坑：（规避项）为验收截取含目标的测试帧时，全片检出数最多的帧（6 个 car）置信度全部只有 0.26~0.39，很可能是噪声误检；若拿它做"切片检出≥整图检出"的断言基准，断言建立在不稳定误检上，换权重/换环境即飘
- 根因：检出数量包含误检贡献，数量最大化不等于"确定存在的真实目标"最大化；验收断言的基准帧本身必须稳定可复现
- 对策：探测候选帧时按"高置信（如 ≥0.8）目标数"排序选帧，而非按总检出数；检出数多但置信度整体偏低的帧只作参考，并在备注中说明选帧依据
- 适用范围：一切需要从历史素材截取基准帧/基准样本的验收任务

## 2026-09-12 验收口径依赖硬件环境时，先探测再选口径，不采信派活描述
- 坑：（规避项）派活 prompt 写明"本机为无 N 卡 CPU 环境"并据此给出降级版验收线，实测 `torch.cuda.is_available()` 却为 True（RTX 3060 Laptop），若直接按"无 N 卡"口径实施，FP16 路径与对应达标线都会被错误跳过
- 根因：派活方对执行机硬件的假设可能过期或张冠李戴；验收口径（用哪条达标线）本身是环境属性的函数，不能凭描述选定
- 对策：动手前先跑环境探测命令（cuda 可用性、GPU 型号、关键库版本）并留痕；探测结果与派活描述冲突时按实测环境执行对应口径，并在交付备注/报告中如实写明偏差
- 适用范围：一切验收标准随硬件/环境分档的任务（推理精度、设备后端、性能达标线）

## 2026-09-12 跳帧外推三原则：不计数、不续鬼、速度按真实帧间隔折算
- 坑：（规避项）跳帧期间用轨迹外推渲染中间帧时，若外推帧照常推进"连续存在帧数"，确认会被虚增提速；若对最近检测帧已消失的轨迹继续外推，画面会出现幽灵框；若速度直接用相邻检测帧位移当"每帧速度"，间隔 N>1 时外推会放大 N 倍飞框
- 根因：外推是"无观测的估计"，观测计数、存活判定、速度量纲都必须锚定在真实观测帧上，混用估计与观测会破坏下游不变量
- 对策：外推帧只平移位置，不推进 age/确认状态、不产生确认事件（确认只认实测帧）；仅外推"最近一次检测帧仍被观测到"的轨迹；速度 = 相邻两个实测帧中心位移 / 实际帧间隔，外推框越界钳制到画面内；多组基准共享同一模型实例时，组间必须 reset 清理 predictor，防止跟踪器内部轨迹跨组接续
- 适用范围：一切"抽稀处理 + 中间步估计补偿"的流水线（跳帧检测、降采样跟踪、关键帧插值）

## 2026-09-12 评估类交付：0 检出先分因，报告数字锚定最终落盘文件
- 坑：多高度评估中某档位素材全程 0 检出，初看像检测失效；另外报告中引用的 FPS/耗时来自第一次运行，重跑复现后数字漂移，与落盘 JSON 不一致
- 根因：0 检出有两类成因——"推理失效"与"类别语义不匹配"（实测素材中弯腰劳作者被 COCO 预训练权重全判为 sheep/cow，被 target_classes 过滤，属系统正确行为），不探测原始类别分布无法区分；而计数类指标（轨迹数/比率）在确定性管线下逐次复现一致，耗时/速度类指标随负载逐次漂移
- 对策：0 检出时先用裸模型 + 全类别 + 低置信度对抽样帧探测原始检出分布，把"过滤后为空"与"推理失效"分开下结论；评估报告中的数字一律以最终一次运行落盘的 JSON 为准，漂移敏感的耗时/FPS 在定稿前与最终 JSON 逐项对账；重跑刷新日志时用 PYTHONUTF8=1 保证日志全文可读（中文不乱码）
- 适用范围：一切无真值的检测/评估类任务与"报告数字须与运行产物交叉吻合"的验收

## 2026-09-12 docx 排版类交付：确定性分页 + 结构自检 + Word COM 实测页数三重验证
- 坑：（规避项）python-docx 无法控制也无法获知 Word 渲染页数，若依赖 Word 自动排版，"总页数=60""每页>=50 行"这类硬指标在脚本侧无法自检兑现；另外首版统计范围断言把"排除目录下存在 .py 文件"误判为"混入清单"（对永远非空的全集取 not 作条件），造成假 FAIL
- 根因：docx 是流式格式，页数由渲染端按页面度量决定，脚本侧不写死分页就只能猜；范围类断言的对象应是"排除集与清单的交集"，而不是排除集本身
- 对策：按固定行数切块 + 每页末尾手动分页符做确定性分页；动手前先探测语料行宽分布（全角=2 单位）再定版式参数，保证单页视觉容量 > 单页逻辑行数上限（含超长行折行余量），逻辑页数即渲染页数。自检分两层：脚本侧逐项断言逻辑分页/页眉文字/页码域/逐行内容一致，再用 Word COM（PowerShell 临时 .ps1 以 UTF-8 BOM 落盘传中文路径）ComputeStatistics(2) 实测渲染页数交叉核对；COM/渲染工具不可用时降级为逻辑判定并注明。范围断言统一写成"交集为空"
- 适用范围：一切"固定页数/每页固定行数/固定页眉页码"的文档排版导出任务（软著代码文档、材料汇编等）

## 2026-09-12 文档截图证据：默认参数全片无事件时，探测定阈值 + 临时运行器隔离产出
- 坑：手册需要"徘徊告警触发"的真实截图，但默认阈值（5s）下全片最长停留仅 2.2s，告警 0 起，直接跑 GUI 到任何帧都截不到告警态画面
- 根因：事件触发是"素材内容 × 判定参数"的联合属性；文档类证据与功能验收一样受此约束，且 GUI 无配置项命令行入口，调阈值不能改 src/config.py（会污染其他任务约定）
- 对策：两段式——先用真实管线探测脚本全片扫描（阈值调低），拿到真实告警帧号；再写临时运行器以"monkeypatch 模块级 Config 类"的方式覆盖阈值与归档目录（不动源文件），驱动真实 GUI 跑到告警帧 dump 画面，归档输出隔离到独立目录避免混入正式 archive/。探测脚本与运行器验完即删，截图与归档产物留作引用；手册中如实注明演示阈值
- 适用范围：一切需要"特定事件状态画面"作为文档/验收证据、而默认参数下素材不产生该事件的任务

## 2026-09-12 PowerShell 调 Word COM 两坑：中文路径编码与 COM 命名参数
- 坑：.ps1 内含中文路径即使补 UTF-8 BOM 仍被按 GBK 误解析；改用 ASCII 路径后 `Documents.Open(path, ReadOnly=$true)` 又报"缺少表达式"解析错误
- 根因：PowerShell 5.1 对脚本文件编码的判定与 BOM 处理有环境差异；且 COM 互操作方法的参数不支持 C# 风格命名参数语法，必须按位置传参
- 对策：中文文件先复制为 ASCII 临时名再打开（测完删除副本）；COM 方法调用一律位置参数（如 Open(path, $false, $true)），布尔按文档顺序对应 ConfirmConversions/ReadOnly
- 适用范围：Windows 下用 PowerShell/COM 自动化 Office 处理含中文路径文档的验收任务

## 2026-09-12 文档中的"真实内容示例"必须逐字段摘自产物文件，禁止凭格式印象手写
- 坑：手册"告警日志 CSV 真实内容示例"表自称截取自运行产物，其中两行字段（位置坐标、置信度）是按 CSV 格式印象手写填入的，与真实 alert_log.csv 不吻合，被判定数据编造打回
- 根因：写作时把"示例"当成了格式演示来写，但标题自称"真实内容"，验证方会逐字段与产物对账；凭记忆/格式印象补全的字段必然失真
- 对策：凡自称"真实/实录/截取自"的内容，写作时直接打开产物文件逐字段复制（含时间戳、坐标、置信度、文件名），一处都不手写；若只想演示格式，就明确写成"格式为 ..."的占位描述并指向真实截图/文件，不给出具体数字。交付前 grep 全文中的时间戳/坐标/长数字，逐个回查产物出处
- 适用范围：一切引用运行产物作为"真实示例"的文档/报告类交付物

## 2026-09-12 清单/索引类交付物：文件元数据当次实测，不转引工具日志
- 坑：（规避项）编写引用既有产物的清单类文档时，若从导出脚本的日志/自检输出中转引文件大小等元数据，可能与交付目录落盘文件的实际值不一致（实测同一 docx：脚本自检日志报 96851 字节，磁盘 stat 实测 97470 字节）
- 根因：导出/自检日志记录的是其运行当刻或内存中重建产物的测量值，落盘文件可能随后被重写或测量口径不同；清单自称"实测值"就会被验证方按磁盘文件逐字段对账
- 对策：凡清单/索引中出现的文件名、大小、页数、日期依据，一律在编写当次对交付目录实际文件重新实测（大小用 stat、页数用 Word COM ComputeStatistics，方法沿用 docx 排版条目），并在文档中写明实测方式与实测日期；禁止从旧日志、旧报告转引数字
- 适用范围：一切汇总/索引既有产物并自称"实测"的文档类交付物（材料清单、汇编目录、交付说明）

## 2026-09-12 引用易再生产物的元数据：以最终交付时刻实测为准，并在文档中提示漂移风险
- 坑：材料清单编写时对落盘文件当场 stat 实测（97,470 字节），但该 docx 在清单定稿前一刻被重新保存（96,851 字节），清单沿用了重存前的实测值，验证方按当前文件对账判 FAIL——即使"当次实测"属实，产物再生仍使已测值失效
- 根因：可再生成产物（脚本导出的 docx 等）的元数据不是稳定属性，测量时刻与验证时刻之间任何一次重导出/重保存都会造成漂移；"当次实测"只保证测量时点正确，不保证交付时点正确
- 对策：引用此类元数据时，把实测放在交付流程的最后一步（产物全部定型后再统一刷新清单中全部大小/页数）；同时在文档中显式提示"以最终导出版本为准、登记前请重新核对"，把漂移风险转移为使用者的复查动作；验证被驳回后修复时，对全部文件重新当场 stat，不沿用任何历史数值
- 适用范围：一切引用可再生成产物（导出文档、构建产物、报表）元数据的清单/索引/说明类交付物

## 2026-09-23 第三方工具的参数组合必须在锁定版本下实测，"框架支持"不等于"任一格式支持"
- 坑：凭"ultralytics 支持 INT8 量化导出"的通用印象写出 `yolo export format=onnx int8=True`，但锁定版本 8.4.21 的 ONNX 导出合法参数表（export_formats()）不含 int8，validate_args() 直接 assert，该实验命令在任何同版本环境必然失败，验证方实测打回
- 根因：大型框架的能力常按子路线分列（每种导出格式一张合法参数表），"框架支持某能力"与"我选的这条路线支持该能力"是两回事；凭文档记忆组合参数，锁定版本下必现的不兼容只有实测才能暴露
- 对策：交付封装第三方 CLI/SDK 的参数组合前，在锁定版本环境做最小真实验证——先源码级核对合法参数表（直接调用其校验函数，如 validate_args()），再干跑确认死点位置：可接受的死点是"缺依赖/缺硬件"（目标环境可补齐），不可接受的死点是"参数非法"（必然失败）；多条备选路线（TensorRT/openvino/onnxruntime）的取舍原因必须写进注释，禁止无声选择
- 适用范围：一切封装第三方 CLI/SDK 参数组合的脚本（模型导出、格式转换、编解码、云 CLI 等）

## 2026-09-23 跨平台交付物三坑：文件名大小写、行尾、路径形式——本机跑通不代表 Linux 能跑
- 坑：bash 脚本在 Windows Git Bash 冒烟全绿，上到 Linux 实例三处齐炸：内置数据集名 `visdrone.yaml` 实为 `VisDrone.yaml`（大小写敏感）；脚本 CRLF 行尾被 Git Bash 文本模式透明吞掉，Linux 原生 bash 直接 `$'\r'` 语法错误全面崩溃；`/c/...` 形式路径 Windows 原生 python/yolo 不可解析
- 根因：Windows 文件系统大小写不敏感、MSYS 文本模式吞 `\r`、MSYS 路径自动翻译，三层兼容假象叠加，使"本机验证通过"对 Linux 目标环境没有证明力
- 对策：目标环境是 Linux 的交付物，本机验证后必须在 Linux 侧做一次最小真实运行（冒烟即可）；交付前显式核查三类假设——引用外部文件名的大小写以目标环境实际为准（包内文件用 ls 核实，不凭印象）；文本交付物行尾统一 LF（file 命令核验）；跨边界的绝对路径用 cygpath 类工具转原生形式
- 适用范围：一切 Windows 开发、Linux 执行的脚本/配置/数据文件引用

## 2026-09-23 产物路径以框架运行时实际行为为准，不按参数字面意义推断
- 坑：给 yolo 训练传 `project=runs`（相对路径），按字面印象应落 `runs/<name>/`；但 ultralytics 8.4.21 `get_save_dir` 对相对 project 隐式前置 `RUNS_DIR/task/`，真实落盘 `runs/detect/runs/<name>/`，下游按字面定位产物必然失败，失败残留还触发 name 自增加剧混乱
- 根因：框架对入参有隐式规范化（相对路径加前缀、同名加序号、缺省补默认），这些派生只在运行时才确定；按参数字面意义推断产物位置等于猜
- 对策：产物路径一律以运行时实际值为准——优先解析框架自己打印/落盘的 save_dir（日志中 `save_dir=` 行、`results.save_dir`），把"目录扫描定位"降为兜底并加存在性+前缀校验防串场；接入框架新参数前亲读其派生函数源码（get_save_dir/increment_path 级别），不留"应该如此"
- 适用范围：一切依赖框架隐式路径/命名派生的产物定位（训练输出、导出文件、检查点、日志目录）
