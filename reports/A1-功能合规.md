# 功能与合规验证报告 A1

## 第 1 次测试

### 逐项检查与证据

**1. 目录结构（plan.md"目录约定"）——通过**
实测 `ls` 项目根目录：`agents/ archive/ copyright/ data/ docs/ logs/ reference/ reports/ src/ tests/(含 assets/) tools/ weights/` 全部存在，另有 `plan.md`、`lessons.md`、`main-log.md`、`requirements.txt`。与 plan.md 目录约定一致。

**2. `pip install -r requirements.txt` 成功——通过**
命令：`python -m pip install -r requirements.txt`（D:\Python3.14.2）。输出全部为 `Requirement already satisfied`（ultralytics>=8.4、opencv-python>=4.8、numpy>=1.24、Pillow>=10.0 及其依赖链），无任何 ERROR，正常结束。requirements.txt 内容与 plan.md 技术约定的技术栈一致。

**3. 配置项完整输出——通过**
命令：`python -c "from src.config import Config; c=Config(); print(c.to_dict())"`。输出为含 25 个键的字典，验收要求的各项均在：
- 中央/边缘区比例：`center_zone_ratio_w=0.5`、`center_zone_ratio_h=0.5`
- 两区置信度阈值：`center_conf_threshold=0.4`、`edge_conf_threshold=0.5`
- 确认帧数 N：`confirm_frames=3`（另有 `iou_threshold=0.3`）
- 警戒线坐标：`warning_line=(0.0, 0.5, 1.0, 0.5)`
- 归档目录：`archive_dir="archive"`（另有 `alert_log_name`）
- 权重路径：`weights_path="yolov8n.pt"`（配置项化，符合"权重路径走配置项"约定）

**4. 日志基础设施——通过**
`src/logger.py`（62 行）提供 `get_logger()`，控制台+文件双输出，文件落 `logs/eagle_eye_YYYYMMDD.log`；实测运行 A2/A3 命令后 `logs/eagle_eye_20260912.log` 有对应日志写入，功能真实生效。

**5. 代码归属与交付目录合规——通过（附轻微观察项）**
`reference/` 目录为空（仅 .gitkeep），`src/` 无参考代码原文复制；`src/`、`tools/` 内无权重/视频等大文件。
观察项（轻微瑕疵，不判 FAIL）：`yolov8n.pt`（6.5MB）位于项目根目录而非 `weights/`。该文件系 ultralytics 开发联调自动下载到工作目录所致，plan.md 明确允许开发联调使用该权重，且软著代码量统计/导出范围仅含 `src/`+`tools/`，不影响交付合规。建议后续整理时移入 `weights/` 并同步配置项。

**6. 中文注释规范——通过**
`src/config.py`、`src/logger.py` 注释与 docstring 均为中文且写明功能意图，符合 plan.md 技术约定。

### 判定：PASS
