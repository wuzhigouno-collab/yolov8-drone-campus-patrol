# 功能与合规验证报告 A11

## 第 1 次测试

验证时间：2026-09-12 14:09–14:12（本机时间）。验证方式：全部独立执行，未采信生产方声明；验证后独立核对的实测数据与生产方声明（300 帧确认轨迹 7 条、ID 切换 1 次、阈值 3）一致。

### 逐项结论

1. **真实执行 `python tests/test_tracker.py --video ".../5G智慧农业素材.mp4"`：通过，退出码 0。**
   - 实测输出（尾段摘录）：`视频跟踪验收结束：共处理 300 帧，后端 bytetrack；确认轨迹 7 条（确认事件 7 次）；跟踪轨迹 9 条、ID 切换 1 次（阈值 <=3）；最长尾迹 30 点；越线事件 0 起；徘徊告警 0 起`，随后 `PASS: tests/test_tracker.py 全部断言通过`，EXIT_CODE=0。
   - ID 一致性断言真实存在且生效：tests/test_tracker.py:469-527 采用 MOT IDSW 代理口径（参照轨迹缺席时同帧 IOU>=0.5 身份移交记 1 次切换，`switches <= args.id_switch_threshold`），阈值默认走 Config.track_max_id_switch=3；实测发生 1 次切换（帧230 #30→#34，IOU=0.70），断言通过。另断言 backend_seen=={"bytetrack"}（全程未降级）、同一 ID 只确认一次、consecutive 逐帧 +1、尾迹长度>=2。
   - 徘徊告警用例（合成轨迹 10 组）全部通过：核心用例 2 在帧 50 触发恰好 1 次徘徊告警（停留 2.00s，阈值 2.0s）；用例 9 归档证据：CSV 表头 [时间戳, 事件类型, 类别, 置信度, 位置, 截图路径]，数据行 `['2026-09-12 14:09:54.497', '徘徊告警', 'person', '0.85', '(500, 400)']`，截图文件真实存在（测试断言 `os.path.isfile(row[5])` 通过）。
2. **目检 tests/a11_gui_frame_tracks.png：通过。** 图中可见检测框带目标 ID（car 0.36 #5、car 0.31 #7、car 0.34 #19）、品红色轨迹尾迹连线与 STAY x.xs 停留时长标注、CENTER ZONE / LOITER ZONE 区域框与 WARNING LINE。文件时间戳 14:04:01，与生产方运行日志（a11_tracker_run.log 13:59）时序吻合。
3. **回归：通过。** 独立执行 `python tests/test_confirm.py`（EXIT=0，7 组用例 PASS）与 `python tests/test_line.py`（EXIT=0，13 组用例 PASS），IOU 降级路径行为不变；test_tracker.py 内部亦以子进程回归并通过。test_tracker.py:338-360 用例 10 确有 bytetrack 后端驱动 LineCounter 的跟踪模式用例（穿线事件正常产生、track_id=7 保持、计数一致），实测 PASS。
4. **代码核查：通过。** tracker.py 直接复用 src.frame_confirm.ConfirmResult（records/confirmed/just_confirmed 结构与 A5 一致），backend 以 BACKEND_BYTETRACK="bytetrack" 与 BACKEND_IOU_FALLBACK="iou-fallback" 区分；降级路径双重保留（Config.tracking_enabled=False 配置降级 + model.track() 异常自动降级为内部 FrameConfirmer）；徘徊区域（loitering_zone）与停留阈值（loitering_duration_sec）走 config.py（第 118-125 行），loitering.py 构造时取快照，测试用例 7 对照验证阈值 1.0s 触发/3.0s 不触发、区域不含锚点不触发；app.py 增量（跟踪开关、徘徊开关、尾迹叠加、徘徊告警归档）经 ast 语法解析通过且全部回归测试绿。
5. **合规：通过。** `src/`、`tools/` 下无 .pt/.mp4/.onnx 等权重/视频大文件（find -size +1M 无结果）；`src/` 对 hw5/d2-city 零匹配，无 reference/ 原文复制（tools/coco2yolo.py 仅注释提及参考来源，为 A9 已验交付物）；A11 新模块 tracker.py/loitering.py 为自研封装，注释/docstring 均为中文。

### 环境备注

- 本机 lap 包已安装（`import lap` 正常），首跑 model.track() 联网装包的说法成立；本次复跑无网络依赖，结果可复现。
- `--max-frames 150` dump 图无目标一事不影响验收：素材目标仅在 88–132 帧的说法与补交的 131 帧 a11_gui_frame_tracks.png（可见 3 个带 ID 目标）自洽，目检以该图为准。

### 判定：PASS

## 第 2 次测试

### 判定：PASS
