# 功能与合规验证报告 A13

## 第 1 次测试

### 判定：PASS

验证时间：2026-09-12 15:06–15:12（验证 Agent 独立执行，未采信生产方声明）

#### 逐项证据

**1. 环境确认与达标线判定**
- 实跑 `python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0) ...)"` 输出：`True NVIDIA GeForce RTX 3060 Laptop GPU`。
- 本机有 N 卡 → 适用"综合 FPS 提升 ≥30%"达标线（plan.md A13 / tools/benchmark.py:188 同口径）。

**2. benchmark 真实执行（退出码 0）**
- 命令：`python tools/benchmark.py --video "C:/Users/OS/Desktop/farm_guard/videos/5G智慧农业素材.mp4" --max-frames 300 --interval 3`（落盘 --out 为验证临时文件，核对后已删除），EXIT_CODE=0。
- 实测输出（2026-09-12 15:06:52–15:07:06）：
  - 优化前（FP32 + 逐帧检测）：300 帧 / 7.27s / **41.27 FPS** / 检出总数 63 / 帧均 0.210 / 确认事件 4
  - 优化后（FP16 生效 + 跳帧间隔 3）：300 帧 / 3.99s / **75.26 FPS** / 检出总数 57 / 帧均 0.190 / 检测帧 100 实测检出 19 / 确认事件 2
  - 日志明确"配置 fp16=True 且检测到 CUDA GPU：启用 FP16 半精度推理"（FP16 真实生效）
- 独立核算：(75.26-41.27)/41.27 = **+82.4% ≥ 30% 达标**；(0.210-0.190)/0.210 = **-9.5% ≤ 10% 达标**。与生产方声称的 +82.6% / -9.5% 一致（绝对 FPS 41.27 vs 40.79 属正常运行抖动，比例与计数完全吻合）。

**3. `docs/性能测试.md` 数据一致性（时间戳/数值交叉验证）**
- 文档数值（40.79 / 74.50 / +82.6% / -9.5% / 63 / 57 / 19 / 确认 4→2）与 `tests/a13_benchmark.json` 逐项完全一致（fps 40.79、74.5，fps_speedup_pct 82.64，avg_records_drop_pct 9.52）。
- 原始日志 `tests/a13_benchmark.log` 时间戳 15:02:31–15:02:45，与 JSON 文件 mtime 15:02:45 吻合；文档 mtime 15:04:23 在运行之后，时序合理，无编造迹象。
- 本人 15:07 独立复跑结果（41.27/75.26/63/57/19/4→2）与文档口径全部对得上。

**4. 代码核查**
- fp16 / detect_interval 走 Config：`config.py:160`（fp16，默认 True）、`config.py:168`（detect_interval，默认 1）。
- 无 CUDA 自动降级 FP32 + 日志说明逻辑存在：`src/detector.py:18-49` `resolve_fp16()` 三分支判定（含"自动降级为 FP32"文案，且不修改配置值）；`src/detector.py:93-94`、`src/tracker.py:119-120` 初始化时记日志。
- 跳帧期间轨迹外推不推进确认计数：`src/tracker.py:341-417` `extrapolate()` 不执行推理，records 标记 `extrapolated=True`、`just_confirmed=False`、`consecutive=state["age"]`（age 不递增、不产生确认事件）；仅最近一次检测帧仍观测到的轨迹参与外推（防幽灵框，`tracker.py:386-389`）。
- GUI 跳帧开关存在：`src/app.py:517` "启用跳帧检测加速"复选框（默认值由 `config.detect_interval > 1` 决定，app.py:236）；跳帧判定与外推调用 `app.py:684-703`；跟踪未开启/降级时一次性日志提示并按逐帧处理（app.py:686-693）。

**5. 单测与回归（退出码均 0）**
- `python tests/test_accel.py`：EXIT=0，12 项断言全 PASS——含 FP16 降级分支（test_fp16_fallback_without_cuda：无 CUDA 降级 FP32、文案含"降级"、不改配置值）、外推位置（test_extrapolate_position：中心 (170,130)→(190,130)、外推帧零推理调用）、外推不计数（test_extrapolate_does_not_advance_confirm：consecutive 保持 2、确认帧号为真实检测帧 4）。
- `python tests/test_zone.py`：EXIT=0（全部 4 断言通过）。
- `python tests/test_confirm.py`：EXIT=0（全部 7 断言通过）。
- `python tests/test_line.py --video ".../5G智慧农业素材.mp4"`：EXIT=0（处理 3712 帧，穿线事件 4 起 进1/出3，"PASS: 合成单测与真实视频验收全部通过"）。
- `python tests/test_tracker.py --video ".../5G智慧农业素材.mp4"`：EXIT=0（300 帧，bytetrack 后端，ID 切换 3 次 ≤ 阈值 3，"PASS: 全部断言通过"）。
- GUI 冒烟：`python src/app.py --video ".../5G智慧农业素材.mp4" --max-frames 120 --dump-frame tests/a13_gui_frame_v.png`，EXIT=0；日志显示帧 103/108 目标确认并归档告警截图，达 120 帧自动退出；ReadMediaFile 抽查 `tests/a13_gui_frame_v.png`（1920x1080，15:11:19 真实生成）：检测框 + 跟踪 ID + "car 0.31 #14 confirm 2/3" 标注、区域边框、警戒线渲染正常。

**6. 合规**
- `src/`、`tools/`、`docs/` 下无 .pt/.pth/.onnx/.mp4/.avi 等权重/视频大文件（find 扫描为空）；`src/`、`tools/` 无 >2M 文件。
- 权重 yolov8n.pt 归置于 `weights/`（plan.md 约定排除目录），未混入交付目录。
- `src/` grep 无 hw5/reference 原文痕迹；`tools/coco2yolo.py` 仅在 docstring 中说明"与参考实现相比按本项目需要重写"（A9 已验证项），reference/hw5-code 未复制进 src/tools。

## 第 2 次测试

### 判定：PASS
