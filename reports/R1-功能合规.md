# 功能与合规验证报告 R1

## 第 1 次测试

### 判定：PASS

验证时间：2026-09-23 18:45–18:50（验证 Agent 独立执行，全部命令本人重跑，未采信生产方声明与其留存日志；生产方日志仅作交叉比对对象）

#### 逐项证据

**1. 既有 8 项测试本人重跑全 PASS（验收标准 1）**

| 测试 | 命令 | 结果 | 关键产出与生产方基线日志（r1_baseline_*.log）比对 |
|---|---|---|---|
| test_zone | `python tests/test_zone.py` | EXIT=0，4 组用例 PASS | 边缘 0.35 目标 阈值0.5过滤/0.3保留、中央区两场景一致，与基线逐行一致 |
| test_confirm | `python tests/test_confirm.py` | EXIT=0，7 组用例 PASS | 第 3 帧确认、阈值 0.30/0.50 对照、断档重计，与基线一致 |
| test_line | `python tests/test_line.py` | EXIT=0，13 组合成用例 PASS | 末尾"PASS: 合成轨迹单元测试 13 组用例全部通过"，与基线一致 |
| test_archive | `python tests/test_archive.py` | EXIT=0 | 合成 5 组 PASS；真实视频端到端"共处理 1059 帧，归档 26 条（目标确认 25 / 穿线 1）"，与基线逐项一致 |
| test_heatmap | `python tests/test_heatmap.py` | EXIT=0 | 合成 5 组 PASS；端到端"处理 478 帧，累积 80 个已确认锚点；密集格亮度 242.8 >> 无目标格 0.0"，与基线逐项一致 |
| test_slicer | `python tests/test_slicer.py` | EXIT=0 | 整图模式检出 4 个（0.9302/0.9189/0.8984/0.8286）、切片模式检出 8 个（0.894/0.8931/0.8569/0.8555/0.8555/0.833/0.8291/0.8027）、最大 pair IOU=0.2391，置信度与基线逐值一致 |
| test_tracker | `python tests/test_tracker.py` | EXIT=0 | 徘徊合成 10 组 PASS；test_confirm.py / test_line.py 子进程回归均退出码 0，与基线一致 |
| test_accel | `python tests/test_accel.py` | EXIT=0 | "全部 12 项断言通过：PASS"，与基线一致 |

**2. grep 独立核验后处理逻辑唯一性（验收标准 2）**
- `names[int(item.cls[0])]`（cls_id 查类别名）全项目仅命中 `src/postprocess.py:43`；
- `item.cls/item.conf/item.xyxy/item.id` 消费、`for item in boxes` 遍历、两种口径 center 计算（`int((x1+x2)/2)` 浮点口径 / `int((box[0]+box[2])/2)` 整数框口径）仅命中 `src/postprocess.py:51-62,86,109-115`；
- `round(conf, 4)` 仅命中 `src/postprocess.py:65`（`src/frame_confirm.py:210` 的 `round(iou, 4)` 系 IOU 匹配记录，非推理后处理）；
- `src/detector.py:127`、`src/tracker.py:202,447`、`src/slicer.py:223` 中 `results.boxes` 仅作为 `collect_detections/collect_track_detections` 调用实参出现，三处只剩调用，slicer 内联后处理副本确已删除。

**3. 新增两个测试文件本人重跑全 PASS + 覆盖抽查（验收标准 3）**
- `python tests/test_postprocess.py`：EXIT=0，9 组用例全 PASS。覆盖抽查：类别过滤（用例1：dog 被滤、target_classes 空/None 不过滤）；字段结构（用例2：四键与键序精确、conf round(4)=0.1235、框 int 取整 [10,20,21,42]、中心 [16,31] 走原始浮点口径——与取整框口径 15 可区分）；空结果（用例3：无框/全过滤/boxes None/无跟踪 ID 均空列表）。
- `python tests/test_class_map.py`：EXIT=0，8 组用例全 PASS。覆盖抽查：10 类全量映射（用例2 日志逐项列出 7 保留类归并）；truck/tricycle/awning-tricycle 丢弃（用例2b：三者返回 None 且不在映射表）；COCO 类名不生效（用例4：含与 VisDrone 同名类 truck/car 也恒等透传不丢弃）；开关关闭不生效（用例5：VisDrone 类名下 pedestrian 不归并、truck 不丢弃）。

**4. class_map 双条件判定本人实测（验收标准 4）**
- 三组合脚本实测（`python -c` 调 `build_class_mapper`）：①开关 True + VisDrone 10 类名 → active=True，pedestrian→person、van→car、motor→motorcycle，三个丢弃类返回 None；②开关 True + COCO 类名 → active=False，truck/person 同名类也恒等透传；③开关 False + VisDrone 类名 → active=False 恒等。三组合均符合"开关 + 类名集合缺一不可"。
- 真实 COCO 权重（weights/yolov8n.pt，80 类）实测：`Config().class_map_enabled=True` 默认值确认；Detector / ByteTrackTracker / SlicedDetector 三路径 `class_map_active` 均为 False（恒等），推理日志输出"模型类别名不全属于 VisDrone 10 类集合（如 airplane）…恒等映射"。
- COCO 权重下三路径输出与重构前一致，由第 1 项既有测试关键产出佐证：slicer 整图 4/切片 8（置信度逐值同基线）、archive 1059 帧 26 条、heatmap 80 锚点 242.8、tracker 回归过——均经 detect/track/slice 真实推理路径产出。
- 备注（非缺陷）：本人临时脚本中同进程先 `track()` 后 `slice` 检出 5 个（单跑 slice 为 8 个，置信度 ±0.001 抖动），系 FP16 半精度浮点随调用历史的正常数值抖动；`python tests/test_slicer.py` 本次实跑与基线逐值一致，测试本身稳定可复现。

**5. 合规核查（验收标准 5）**
- `.gitignore`：mtime 2026-09-23 13:56，早于 R1 生产窗口（基线 18:30 起）；内容指向 docs/实验计划.md §12 排除清单，未被本任务改动。
- `plan.md`：mtime 2026-09-12 16:30，未改动。
- `main-log.md`：mtime 18:44，尾部为编排方日志条目（260923 1823/1831，含"R1 生产完成…派验证"），编排方维护性质；`docs/实验计划.md` mtime 18:13，同为编排方文档（派活口径称 18:2x，实际 18:13/18:44，出入仅作备注，内容与 R1 交付物无关）。
- `reports/` 全部文件 mtime 2026-09-11/12；`docs/` 其余文件与 `tools/` 全部文件 mtime 2026-09-11/12，均未被本任务修改。
- 交付目录无权重/视频混入：`src/` 仅 .py（__pycache__ 除外）、项目根与 `tests/` 新增区 find 无 .pt/.onnx/.mp4/.avi；`weights/` 内 yolov8n.pt/yolo26n.pt 系 plan 约定归置处且被 .gitignore 排除，非混入。
- 交付物时序自洽：基线日志 18:30–18:31 → 源码改动 18:33–18:36（class_map 18:33、postprocess 18:34、config 18:34、tracker 18:36、slicer/detector 18:35）→ 新测试文件 18:38–18:40 → 回归日志 18:40–18:41。

**6. 红线核查（验收标准 6）**
- 生产方留存日志与本人实跑产出逐项交叉比对：r1_reg_slicer（整图 4/切片 8，置信度逐值同）、r1_reg_archive（1059 帧 26 条，确认 25/穿线 1）、r1_reg_heatmap（478 帧 80 锚点 242.8）、r1_new_test_postprocess（9 条"用例 PASS"计数+末尾 PASS 行）、r1_new_test_class_map（8 条+末尾 PASS 行）——全部吻合，日志时间戳与文件 mtime 时序无矛盾，未发现编造迹象。
