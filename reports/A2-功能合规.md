# 功能与合规验证报告 A2

## 第 1 次测试

### 逐项检查与证据

**1. 验收命令重跑（不采信既有输出）——命令本身正常，但帧数不达标**
命令：`python tools/demo_detect.py --video C:/Users/OS/Desktop/farm_guard/videos/farm_guard.mp4 --out %TEMP%/a2_verify_out_frames.json`（默认 --max-frames 100），独立重跑。
实测结果：退出码 0，无异常堆栈；日志"演示完成：共处理 50 帧，检出目标 0 个"。输出 JSON 含 50 条记录，帧序号 0–49 连续，每条均含 `detections` 列表，键集合一致。
**但只处理了 50 帧，未达到验收标准"跑满 100 帧"**。经查证原因在素材：OpenCV 实测该视频 `CAP_PROP_FRAME_COUNT=50`、逐帧读取上限 50 帧——视频本身只有 50 帧。

**2. 严重问题：既有产出 JSON 系编造/张冠李戴——不通过**
既有的 `tests/out_frames_100.json` 与 `tests/out_frames.json`（两文件字节数完全相同，7629B）均声称含 **100 帧记录（frame 0–99）、检出目标 11 个**（frame 76–92 检出 "car"，frame 99 检出 "car"）。
但指定测试视频实际仅 50 帧，`demo_detect.py` 的循环在视频结束时即 break，**frame≥50 的记录在物理上不可能由该验收命令产生**。这些文件不是"用指定测试视频真实运行"的产出，属数据编造（或来自其他视频的张冠李戴），违反通用检查清单"数据不编造"。

**3. 检测功能正向验证——代码本身通过**
默认配置下 50 帧 0 检出，经核实是 `target_classes=["person","car","bicycle"]` 过滤的正常行为而非缺陷：原始模型输出（conf=0.05 对照）在该视频的检出均为 `sheep`（田间弯腰劳作的人被 COCO 预训练模型误分为羊），不属于关注类别故被过滤。
内存中将 `target_classes` 临时改为 `["sheep"]`（不修改任何交付文件）后实测 `Detector.detect()`：单帧检出 12 个目标，结构 `{"cls","conf","box","center"}` 完整正确（样例：`{"cls": "sheep", "conf": 0.605, "box": [891, 554, 932, 603], "center": [912, 579]}`）。
`src/detector.py`（权重加载走配置项、单帧推理、统一结构返回）、`src/video_source.py`（文件/摄像头两源、统一 `read()->frame`、工厂函数）代码审查与运行行为一致，功能正确。

**4. 代码归属——通过**
`reference/` 为空，`src/`、`tools/` 无抄袭源可比对，无违规。

### 判定：FAIL

| # | 严重度 | 位置 | 原因 | 修改建议 |
|---|--------|------|------|----------|
| 1 | 严重 | tests/out_frames_100.json、tests/out_frames.json | 两文件声称 100 帧（frame 0–99）含 11 个检出，但指定测试视频实测仅 50 帧，frame≥50 的记录不可能由验收命令产生，属编造数据/非真实运行产出 | 删除伪造产出，用验收命令对指定视频重跑生成真实 JSON 并如实记录帧数 |
| 2 | 中等 | A2 验收记录 | 实测验收命令仅处理 50 帧（视频本身仅 50 帧），未达到"跑满 100 帧"的验收要求 | 换用 ≥100 帧的测试视频重跑验收命令；或修订 plan.md 验收标准为"视频不足 100 帧时以视频结束为准" |

## 第 2 次测试

> 重测范围：仅验证第 1 次 FAIL 项（数据真实性、帧数达标）；代码功能项第 1 次已确认正常，不重复验证。

### 逐项检查与证据

**1. 上次问题项#2：帧数达标——通过**
plan.md 当前 A2 验收卡指定素材为 `C:/Users/OS/Desktop/farm_guard/videos/5G智慧农业素材.mp4`。
实测该素材：`cv2.VideoCapture` 打开成功，`CAP_PROP_FRAME_COUNT=3712`、25fps、1920x1080，前 120 帧逐帧可读——素材帧数充足，"跑满 100 帧"客观可达。
独立重跑验收命令（输出重定向至 %TEMP%/a2_verify_round2.json，未触碰交付物）：`python tools/demo_detect.py --video C:/Users/OS/Desktop/farm_guard/videos/5G智慧农业素材.mp4 --out <临时路径>`，退出码 0，无异常堆栈，日志"演示完成：共处理 100 帧，检出目标 11 个"。**跑满 100 帧达成。**

**2. 上次问题项#1：数据真实性——通过（独立复跑逐帧比对一致）**
- 交付物 `tests/out_frames.json`（2026-09-12 01:25 生成）：100 条记录，帧序号 0–99 连续无越界（≤ 素材实际 3712 帧），每帧含 `detections` 列表，检出目标 11 个（均为 car，分布于 frame 76–92、99），detection 结构 `{cls,conf,box,center}` 完整。
- 我将独立复跑产出的 JSON 与交付 JSON 逐帧程序化比对：**100 帧的 detections 全部完全一致，0 帧差异**（含每个目标的类别、4 位小数置信度、box、center 逐一相等）。交付数据确由对该素材的真实运行产生。
- 附注（如实说明）：本轮交付 JSON 的检出数值与第 1 次测试中 quarantine 的旧 `out_frames_100.json` 完全相同。结合本次复跑结果可确认：旧文件的数据本身就是对该 5G 素材的真实运行输出，第 1 次 FAIL 的症结是"产出所跑的素材与当时验收卡指定的 farm_guard.mp4（仅 50 帧）不符"（张冠李戴）；现验收卡已修订为明确指定该 5G 素材，同一数据经独立复跑验证可复现，问题闭环。

**3. 旧伪造/不符文件清理确认——通过**
`tests/` 下旧 `out_frames_100.json` 已删除，仅余本次真实产出的 `out_frames.json`（7629B）。`lessons.md` 已追加"产出数据必须真实运行产生，且帧数与素材实际一致"教训条目。

### 判定：PASS
