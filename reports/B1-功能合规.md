# 功能与合规验证报告 B1
## 第 1 次测试
### 判定：PASS

## 第 2 次测试

### 判定：PASS

本轮按派活要求全量重验（不沿用第 1 次结论）：

**1. 自检命令 —— 通过**
`python tools/export_code_doc.py --check` 真实执行，退出码 0；9 项自检全部 PASS：统计范围仅 src/+tools/ 共 20 个 .py（reference/tests/archive/weights 混入 0）；实测总行数 6,067、可排满 121 页，走超量分支取前 30 页+后 30 页；逐页行数最小 50；逻辑 60 页与 Word 实测渲染 60 页一致；页眉、页码右上角、内容一致性（3,017 行逐行比对）、分页符 59 个、导出文件 96,851 字节各项均 PASS。（注：总行数超出 plan.md 目标区间 2500–4000，按任务卡"代码超量时取前 30+后 30"规则处理，脚本已如实说明，不影响判定。）

**2. 代码行数独立统计 —— 通过**
验证方实测：`ls src/*.py tools/*.py | wc -l` = 20 个文件；`cat src/*.py tools/*.py | wc -l` = 6,067 行（逐文件：app.py 1201 … train.py 279），与自检清单逐文件一致。

**3. docx 独立复核（python-docx，不依赖生产脚本） —— 通过**
- 页眉：`鹰眼巡校——无人机校园安全智能巡检系统 V1.0`，含全称+V1.0；
- 页码：页眉含 ` PAGE ` 域 + 右对齐制表位（pos=9638 twips ≈ 17cm，即 A4 版心右缘），页码位于右上角；
- 每页行数：按手动分页符切分共 60 个逻辑页，逐页 50–51 行，全部 ≥50；
- 页面：A4（21.00×29.70cm）。

**4. 内容连续性独立重建比对 —— 通过**
验证方自行按排序规则重建语料（6,067 行、可排 121 页），取前 30+后 30 页与导出正文 3,017 行逐行比对**完全一致**；首页起于 `src/app.py` 文件头，末页止于 `tools/train.py` 的 `main()`，确为"前 30 页+后 30 页连续"。

**5. Word COM 渲染页数独立实测 —— 通过**
验证方自备 ps1（临时目录，UTF-8 BOM）驱动本机 Word：`ComputeStatistics(2)` = **60 页**；Repaginate 后按渲染分页抽第 1/30/31/60 页，行数 52/51/51/51，均 ≥50。

**6. 重导出影响说明**
本轮按派活要求执行 --check 会确定性重导出 docx：重导出前 96,851 字节、后 96,851 字节，字节数未漂移（hash 差异仅 zip 内时间戳；正文逐行已验证一致）。
