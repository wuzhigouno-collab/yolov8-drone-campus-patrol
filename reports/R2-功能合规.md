# 功能与合规验证报告 R2

## 第 1 次测试

### 判定：PASS

验证时间：2026-09-23 19:15–19:20（验证 Agent 独立执行，全部命令本人重跑，未采信生产方声明与其留存日志；生产方日志仅作交叉比对对象。本人实跑原始日志留存于 reports/r2_verify_logs/）

#### 逐项证据

**1. 既有 10 项测试本人重跑全 PASS（验收标准 1）**

| 测试 | 命令 | 结果 | 关键产出与 R1 基线日志（tests/r1_baseline_*.log）比对 |
|---|---|---|---|
| test_zone | `python tests/test_zone.py` | EXIT=0，4 组用例 PASS | 边缘 0.35 目标 阈值0.5过滤/0.3保留、中央区两场景一致，与基线一致 |
| test_confirm | `python tests/test_confirm.py` | EXIT=0，7 组用例 PASS | N=3 第 3 帧确认、断档重计、N=2 配置生效，与基线一致 |
| test_line | `python tests/test_line.py` | EXIT=0，13 组合成用例 PASS | 末尾"PASS: 合成轨迹单元测试 13 组用例全部通过"，与基线一致 |
| test_archive | `python tests/test_archive.py` | EXIT=0 | 合成 5 组 PASS；真实视频端到端"共处理 1059 帧，归档 26 条（目标确认 25 / 穿线 1）"，与基线逐项一致 |
| test_heatmap | `python tests/test_heatmap.py` | EXIT=0 | 合成 5 组 PASS；端到端"处理 478 帧，累积 80 个已确认锚点；密集格亮度 242.8 >> 无目标格 0.0"，与基线逐项一致 |
| test_slicer | `python tests/test_slicer.py` | EXIT=0，4 组用例 PASS | 整图模式检出 4 个（0.9302/0.9189/0.8984/0.8286）、切片模式检出 8 个（0.894/0.8931/0.8569/0.8555/0.8555/0.833/0.8291/0.8027）、最大 pair IOU=0.2391，置信度与基线逐值一致 |
| test_tracker | `python tests/test_tracker.py` | EXIT=0 | 徘徊合成 10 组 PASS；test_confirm.py / test_line.py 子进程回归均退出码 0，与基线一致 |
| test_accel | `python tests/test_accel.py` | EXIT=0 | "全部 12 项断言通过：PASS"，与基线一致 |
| test_postprocess | `python tests/test_postprocess.py` | EXIT=0，9 组用例 PASS | R1 新增测试，本次回归通过 |
| test_class_map | `python tests/test_class_map.py` | EXIT=0，8 组用例 PASS | R1 新增测试，本次回归通过 |

**2. grep 独立核验归属唯一性（验收标准 2）**
- `YOLO(`：src/ 内实际调用仅 `src/engine.py:118`（`src/engine.py:15` 为 docstring 提及）；`from ultralytics import YOLO` 仅 `src/engine.py:116`（延迟导入）。
- `resolve_fp16`：定义仅 `src/engine.py:37`，实际调用仅 `src/engine.py:135`；`src/detector.py:20-26` 仅兼容转出（import + `__all__` + 注释），无定义无调用；`src/config.py:159` 为注释提及，且经兼容路径 `src.detector.resolve_fp16` 仍可导入，表述不失实。
- `build_class_mapper(`：定义于 `src/class_map.py:80`，src/ 内调用仅 `src/engine.py:142`。
- 权重路径解析：`config.abs_path(config.weights_path)` 仅 `src/engine.py:113,150`；src/ 内其余 `abs_path` 命中均为 archive_dir/log_dir（archiver.py:83、app.py:314,1138、logger.py:52），与权重无关；`config.py:39` 为配置项默认值定义，非解析。

**3. 装配单例本人实测（验收标准 3）**
- 本人自写临时脚本（reports/r2_verify_logs/tmp_check_app_assembly.py，验完已删）：真实 `tk.Tk()` + `EagleEyeApp(root, args)` 装配，断言 `app.detector.model is app.tracker.model is app.slicer.model`（is 判定）、三者 `engine is app.engine`、names 同源；root logger 捕获全程"正在加载 YOLO 模型权重"日志恰 1 条且指向 weights/yolov8n.pt。输出：`PASS: EagleEyeApp 装配单例成立——三路径同一 model/同一 engine；权重加载日志恰 1 条`。
- 装配代码审阅佐证：`src/app.py:255-267` `self.engine = DetectionEngine.shared(self.config)` 后 Detector/ByteTrackTracker/SlicedDetector 均以 `engine=self.engine` 显式注入。

**4. E6 就绪本人实测（验收标准 4）**
- `python tests/test_engine.py`：EXIT=0，5 组用例全 PASS。含换权重用例（用例5）：Config 仅改 `weights_path = weights/yolo26n.pt`，断言三路径共享新引擎/新模型（is 判定）、与默认权重引擎不同实例、names 三路径一致且与直接 `YOLO(weights/yolo26n.pt).names` 逐值相同（80 类）——本人重跑日志确认"用例5 PASS"与末尾"全部 5 组用例断言通过 / PASS: tests/test_engine.py 全部断言通过"。

**5. 兼容行为读码抽查（验收标准 5）**
- tracker.py model 注入路径：`src/tracker.py:105-107` 保留"传 model 则 `DetectionEngine(config, model=model)`（不加载权重），否则取 shared() 单例"分支；test_engine 用例2 以 `ByteTrackTracker(config, model=DummyModel)` 注入伪造模型断言通过（本人重跑），test_tracker.py:394 真实模型注入路径随测试通过佐证。
- 切片让位 ByteTrack 互斥：`src/app.py:681-684` `use_tracking = enable_confirm and enable_tracking and tracker is not None and not use_slicing`，互斥条件未被改动；启动期提示日志（app.py:630-635）与播放循环注释（app.py:678-680）一致。
- IOU 降级路径：`src/tracker.py:181-193`（`_degrade_to_iou` 一次性降级+日志）、`:240-242`（update 走 `_fallback`）、`:381-383`（extrapolate 降级退化为真实检测）均在；test_tracker 内 test_confirm/test_line 子进程回归本人重跑退出码 0。

**6. 合规核查（验收标准 6）**
- `.gitignore`：mtime 2026-09-23 13:56，早于 R2 生产窗口（19:06 起），未被本任务改动。
- `plan.md`：mtime 2026-09-12 16:30，未改动。`lessons.md`：mtime 2026-09-12 15:52，未改动。
- `main-log.md`：mtime 2026-09-23 18:53，处派活口径允许的 18:4x–19:0x 编排方维护窗口，不算违规。
- `src/config.py`：mtime 2026-09-23 18:34，系 R1 任务窗口改动，R2 窗口（19:06–19:13）内未触及。
- `tools/` 全部文件 mtime 2026-09-12；`docs/` 内 实验计划.md mtime 18:13（编排方文档，早于 R2 窗口）、其余 mtime 2026-09-12；`reports/` 内除本人验证产物（本报告与 r2_verify_logs/）与既有报告外无 R2 窗口新增。
- 交付目录无权重/视频混入：find `src tools docs copyright data agents tests` 无 .pt/.onnx/.engine/.mp4/.avi；`weights/yolov8n.pt` 与 `weights/yolo26n.pt` 系 plan.md 约定归置处，非混入。
- 交付物时序自洽：源码改动 19:06–19:08（engine 19:06、detector 19:06、slicer/tracker 19:07、app 19:08）→ 新测试 19:10 → 回归/装配日志 19:10–19:12。

**7. 红线核查（验收标准 7）**
- 生产方留存日志与本人实跑逐项交叉比对：r2_reg_slicer（整图 4/切片 8，置信度逐值同、pair IOU=0.2391 同）、r2_reg_archive（1059 帧 26 条，确认 25/穿线 1）、r2_reg_heatmap（478 帧 80 锚点 242.8）、r2_new_test_engine（5 组用例 PASS+末尾 PASS 行）、r2_check_app_assembly（加载日志 1 条、GUI 初始化完成）——全部吻合。
- 日志内部时间戳与文件 mtime 逐文件核对无矛盾（r2_new_test_engine 19:10:55→19:10:58 / mtime 19:10:58；r2_reg_slicer 19:11:12→19:11:16 / mtime 19:11:16；r2_reg_archive 19:11:22→19:11:47 / mtime 19:11:47；r2_reg_heatmap 19:12:19→19:12:31 / mtime 19:12:31），未发现编造迹象。
