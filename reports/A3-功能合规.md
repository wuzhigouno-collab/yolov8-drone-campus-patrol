# 功能与合规验证报告 A3

## 第 1 次测试

### 逐项检查与证据

**1. 启动无报错、正常加载视频并播放——通过**
命令：`python src/app.py --video C:/Users/OS/Desktop/farm_guard/videos/farm_guard.mp4 --max-frames 10 --dump-frame %TEMP%/a3_verify_frame.png`（app.py 自带自动化验收参数，播到第 N 帧自动正常退出，无需强杀）。
实测：退出码 0，无异常堆栈。日志序列完整：`GUI 初始化完成` → `已打开视频文件（1920x1080，30.00 fps）` → `开始巡检` → 逐帧处理 → `已达到最大帧数 10，自动退出` → `视频源已释放`。视频逐帧播放属实（帧号推进、帧计数正确）。

**2. 界面标题——通过**
`src/app.py:41` 常量 `WINDOW_TITLE = "鹰眼巡校——无人机校园安全智能巡检系统 V1.0"`，经 `root.title()` 设置；运行日志 `GUI 初始化完成：鹰眼巡校——无人机校园安全智能巡检系统 V1.0` 与验收要求逐字一致。

**3. 界面元素齐全——通过（代码审查+运行验证）**
`src/app.py` 实现了验收要求的全部界面元素：视频画面区（Canvas，`_build_video_area`）、视频源选择（路径输入框+"浏览…"文件对话框+摄像头设备号提示，`browse_file`/`create_video_source`）、开始/停止按钮（`start_patrol`/`stop_patrol`，状态互斥切换）、状态栏（`status_var`+帧号 `frame_var`）、告警/检测摘要列表区（Listbox，`_update_alert_list`，有界 300 条）。

**4. 检测框/类别/置信度实时渲染——通过（正向触发实测）**
首次运行时 dump 的第 10 帧无检测框——经查证并非渲染缺陷，而是该测试视频在默认 `target_classes` 过滤下检出为空（田间人物被 COCO 预训练模型误分为 sheep，详见 A2 报告）。
为真实触发渲染管线，验证方编写临时驱动脚本（放系统临时目录，仅内存中放宽 `target_classes`，**未修改任何交付文件**）重跑 GUI：`--max-frames 15 --dump-frame %TEMP%/a3_verify_frame_dets.png`，退出码 0。目检 dump 画面：多个绿色检测框正确叠加在目标上，框上方标签格式为"类别 置信度"（如 `sheep 0.72`、`sheep 0.30`），带底色条，与 `_draw_detections` 代码一致。渲染管线（读帧→detect→标注→Canvas 缩放显示）真实工作。

**5. 代码归属与资源释放——通过**
`reference/` 为空，无抄袭嫌疑；`src/` 无大文件混入。播放结束/窗口关闭路径均释放视频源（日志"视频源已释放"）。

**6. 既有线索交叉验证**
既有 `tests/a3_gui_frame*.png`（无框版 969KB、有框版 2.9MB）与本次实测现象一致（默认配置无框、放宽类别后有框），时间戳与 `logs/eagle_eye_20260912.log` 可交叉对应，未见编造迹象。

### 判定：PASS
