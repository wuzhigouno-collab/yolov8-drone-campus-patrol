# 功能与合规验证报告 A12

## 第 1 次测试

### 判定：PASS

验证时间：2026-09-12 14:37–14:45（验证 Agent 独立执行，未采信生产方声明）

#### 逐项证据

**1. `python tests/test_slicer.py`（退出码 0）**
- 命令实跑输出（时间戳 2026-09-12 14:37:55–14:38:03）：
  - 用例1 PASS：2x2/重叠0.2 区域坐标精确（水平重叠 214px、垂直 120px）、全覆盖；重叠0 相接、1x1 整图、3x2 六片
  - 用例2 PASS（坐标映射单测）：切片内 (10,20,110,120)+偏移(853,480) -> 原图 [863,500,963,600]；越界/负坐标钳制正确
  - 用例3 PASS（跨切片边界合并专项单测）：跨边界目标 IOU=0.9254 合并去重保留高置信框；IOU=0.5 对照场景阈值 0.5 合并 / 0.6 保留；类别感知不互杀
  - 用例4 PASS：整图模式检出 4 个（person 0.93/0.92/0.90/0.83），切片模式检出 8 个（跨切片 0/1/2/3），**8 ≥ 4**；合并后任意两框 IOU<0.5（最大 pair IOU=0.2416）；接口与 Detector.detect() 对齐
  - 末尾输出 `PASS: tests/test_slicer.py 全部断言通过`，EXIT=0
- 专项单测存在性核对：`test_cross_boundary_merge`（tests/test_slicer.py:125）、`test_coordinate_mapping`（:104）均存在且实跑通过。

**2. 画面依据抽查（ReadMediaFile）**
- `tests/assets/a12_slice_frame.jpg`（1920x1080）：内容为室内聚餐场景，画面含 4 个清晰可见的成年人 + 1 名背对镜头的儿童。整图模式检出 4 人（高置信），切片模式检出 8 框。
- 独立复跑两种模式并打印框坐标核对：切片模式多出的 4 框是同一批人在切片水平分界线（y≈480/600）上下被分别检出的上半身/下半身框（如整图框 [142,286,545,1069] 对应切片框 [217,282,539,597]+[146,483,549,1071]），两两 IOU 最大 0.2416 < 0.5，满足"无重复框"断言；检出数 8 ≥ 4 的说法有真实运行依据（数值本人复跑一致，非编造）。
- 生产方留存的 `tests/a12_gui_frame_slice.png`（14:34）显示航拍农田画面下切片模式开启时的检出渲染（car/person 框 + 确认轨迹号），佐证切片路径在 GUI 中真实生效。

**3. 代码核查**
- `src/slicer.py` 无 sahi 依赖：grep import 仅 `src.config` / `src.frame_confirm` / `src.logger` / 延迟导入 `src.detector`，无第三方切片库（slicer.py:22-24,168）。
- 重叠率/NMS 阈值走 Config：`slice_rows/slice_cols/slice_overlap_ratio/slice_nms_iou` 均从 Config 读取快照（slicer.py:178-181；config.py:133-143），测试断言快照与配置一致。
- 接口对齐：`SlicedDetector.detect(frame)` 返回 `[{cls, conf, box, center, slice}]`，与 `Detector.detect()`（detector.py:57-88）契约键完全一致（多一个追溯字段 slice，测试逐键断言）。
- `src/app.py`：切片开关默认关闭（`enable_slice_var = BooleanVar(value=config.slice_enabled)`，app.py:216；config 默认 False）；开启时检测路径走 `self.slicer.detect(frame)`（app.py:636-637）。
- 切片与 ByteTrack 互斥让位：`use_tracking = ... and not use_slicing`（app.py:624-627），开启切片时记录让位日志"切片推理开启期间 ByteTrack 跟踪自动让位（model.track 内部为整图推理，不与切片叠加），多帧确认走 A5 IOU 降级路径"（app.py:573-578），与设计决策一致。

**4. GUI 冒烟（退出码 0）**
- 命令：`python src/app.py --video ".../5G智慧农业素材.mp4" --max-frames 120 --dump-frame tests/a12_gui_frame_v.png`，EXIT=0；日志显示默认配置下走 bytetrack 后端（帧103/115 目标确认告警归档），达 120 帧自动退出并保存标注帧。
- ReadMediaFile 抽查 `tests/a12_gui_frame_v.png`（3.4MB，14:39 真实生成）：区域边框、警戒线、双向计数、"car 0.31 #8 confirm 2/3" 跟踪标注渲染正常，切片默认关闭行为无回归。

**5. 回归（退出码均 0）**
- `python tests/test_zone.py`：EXIT=0
- `python tests/test_confirm.py`：EXIT=0
- `python tests/test_line.py`：EXIT=0（13 组合成用例全部通过，末尾"PASS: 合成轨迹单元测试 13 组用例全部通过"）

**6. 合规**
- src/+tools/ 与 reference/hw5-code/*.py 逐文件 cmp 无相同文件；行级相似度扫描（>20 字符非注释行交集 ≥5）无任何命中，无原文复制。
- 交付目录（src/tools/tests/docs/copyright/agents）无 .pt/.mp4/.onnx/.avi 权重或视频文件；`tests/assets/a12_slice_frame.jpg` 仅 315KB 小素材，符合"只放小段剪辑/小素材"约定。
- `config.slice_enabled = False`（config.py:133）已核实为默认关闭，生产方"测试后还原"的声明属实。

#### 备注（不影响判定的观察项）

- 测试素材为室内聚餐场景帧，并非 plan.md/派活描述中的"高空测试图"（test_slicer.py:179 docstring 亦称"高空测试图"）；素材内容描述与实际画面不符，建议后续修正措辞或换用航拍帧，不影响断言语义。
- 该素材上"切片 8 ≥ 整图 4"的增量来自同一人被切片边界切成上下两个半身影框（合并 NMS 按 IOU<0.5 标准不视为重复），而非整图漏检的小目标被补检；验收断言全部满足，但"提升小目标召回"的效果建议后续用真正含高空小目标的素材再佐证。
- 项目根目录存在 `yolo26n.pt`（5.5MB，Sep 12 13:20，早于本次 A12 交付）：按 plan.md 属 A10 对比实验用品、不进交付主线，A10 尚 ⏳；非 A12 交付物，建议 A10 收尾时归置或清理。

## 第 2 次测试

### 判定：PASS
