# 功能与合规验证报告 P1

## 第 1 次测试

验证时间：2026-09-23（实例 autodl-container-d3044fa0f1-123a0439，RTX 4090）。全部条目由验证方亲自 SSH 实测，生产方证据仅作交叉比对。

### 判定：PASS

### 逐项证据

1. **数据集计数（亲自重跑）** — `ls images/{train,val,test} | wc -l` = 6471 / 548 / 1610；labels 同口径 = 6471 / 548 / 1610；`find labels/<split> -size 0` 空文件 = 0/0/0；逐文件双向比对 images↔labels 缺失 = 0/0/0；du：images 1.9G、labels 39M、VisDrone 总计 2.2G（challenge 296M 单列）。✅
2. **test 口径（亲读实例源码）** — `/root/miniconda3/lib/python3.12/site-packages/ultralytics/cfg/datasets/VisDrone.yaml`：`test: images/test # test-dev images (optional) 1610 images`；download 脚本 urls 列表中 `VisDrone2019-DET-test-challenge.zip` 一行被注释，转换映射仅 `{train, val, test-dev→test}`。`comm -12` 比对 images/test 与 challenge 文件名交集 = 0，challenge 未混入。challenge 目录 1580 张仅存 images、无标注（与官方包一致）。✅
3. **版本核验（亲自跑）** — pip show：ultralytics 8.4.21、torch 2.8.0+cu128、albumentations 2.0.8、tensorboard 2.20.0；`python -c "import tensorrt"` 实测输出 `tensorrt 11.3.0.99`；`torch.cuda.is_available()=True`（CUDA 12.8）；`tmux -V` = tmux 3.2a；`yolo settings` 中 `datasets_dir: /root/autodl-tmp/datasets` 已固化。✅
4. **仓库核验** — `/root/autodl-tmp/eagle_eye`：git log 顶部 `ef27d2a 鹰眼巡校：系统基线与 VisDrone 实验基础设施（阶段 A/B/C）`；分支 `exp/visdrone-v2`；`git status --porcelain` 输出为空（工作树干净；yolov8n.pt 6.2MB 为 val 自动下载产物，被 .gitignore 排除未入库）。✅
5. **进程与计费纪律** — `ps aux | grep -E "yolo|train"` 无残留（输出空）；`tmux ls` = `no server running on /tmp/tmux-0/default`（三个会话均已结束，如实）；`nvidia-smi` 显存占用 0 MiB、compute-apps 为空，无占卡进程。✅
6. **本地证据完备性** — `logs/p1/` 17 个文件全部非空；关键项与实测逐一比对一致：计数（6471/548/1610、空 label 0）、版本号、git 分支/commit、yolo settings、tmux 3.2a、nvidia-smi 空卡；本地 `VisDrone.yaml` 与实例包内文件 diff 完全一致（YAML_IDENTICAL）。✅
7. **val 产物核实** — `runs/detect/val/` 存在（曲线图+混淆矩阵+val_batch 3 组，时间戳 21:39 与下载 val 日志连续）；亲自读 val 日志结果表：`all 548 images / 38759 instances`，**mAP50=0.0404、mAP50-95=0.0192**（yolov8n.pt COCO 权重跨域评 VisDrone 标签，近零为预期的环境 sanity 参照值，不进论文）；日志尾部 `Results saved to /root/autodl-tmp/eagle_eye/runs/detect/val`，全程无训练调用记录。✅
8. **红线检查** — 未发现任何证据文件内容与实例实测不符。✅

### 备注

- val 摘要（环境 sanity 参照，勿入论文）：yolov8n.pt（COCO）@ VisDrone val 548 张，mAP50=0.0404，mAP50-95=0.0192。
- 实例残留物说明（非问题）：`eagle_eye/yolov8n.pt`（val 自动下载，已 gitignore）、`labels/val.cache`（val 扫描缓存）、challenge 目录 296M（生产方声明另存，已核实未并入 images/test）。
