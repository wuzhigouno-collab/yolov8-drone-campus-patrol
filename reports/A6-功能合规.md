# 功能与合规验证报告 A6

## 第 1 次测试

### 逐项检查与证据

**1. 真实视频验收——通过**
命令：`python tests/test_line.py --video "C:/Users/OS/Desktop/farm_guard/videos/5G智慧农业素材.mp4" --max-frames 1100 --evidence-dir tests/line_evidence_v`（为避免覆盖生产方佐证，验证方改用独立目录 `tests/line_evidence_v/`，交付物未动）。
实测：退出码 0；合成单测 13 组先行通过后进入视频模式，输出 `[穿线事件] 帧1059 | t=42.36s | 越线出 | person #64 | 穿线点(746,540) | 累计 进0 出1`，末尾 `PASS: tests/test_line.py 合成单测与真实视频验收全部通过`。
独立复现的事件（`tests/line_evidence_v/cross_events.json`）与生产方声明的第 1 起事件逐字段一致：帧1059、time_sec=42.36、方向 out/出、cls=person、conf=0.692、track_id=64、box=[128,3,1379,562]、cross_point=(746,540)、负侧→正侧；事件字段齐全（帧号、视频内时间、墙上时间、方向中/英文、类别、置信度、轨迹号、目标框、穿线点、侧跳变、原因）。验证方自产佐证 PNG 与生产方 `tests/line_evidence/` 对应文件字节数完全一致（2007519/2061539），运行确定可复现。其余 3 起事件帧号（2025/2043/2048）超出 1100 帧限量未复跑，按验收标准"覆盖到至少一个事件帧号"执行。
人工抽查（帧1059 事件，ReadMediaFile 目检验证方自产佐证帧）：穿线帧中红色目标框锚点（底部中心 (754,562)）位于警戒线 y=540 下方，穿线点标记 (746,540) 落在线上；前 8 帧缓存中最早的 f01051 对照帧显示同一轨迹 #64 的框底（锚点）位于警戒线上方侧。锚点由线上方→下方穿越属实，默认 `line_enter_direction=True` 语义下"上->下=出"，方向判定与画面一致。注：该"person"检测实为画面中手持传感器的人手（COCO 预训练模型对农业素材的检测质量问题），属 A2 检测器范畴，不影响 A6 穿线判定逻辑正确性。

**2. 合成轨迹单元测试——通过**
命令：`python tests/test_line.py`，退出码 0，日志 13 组用例全 PASS，输出 `PASS: 合成轨迹单元测试 13 组用例全部通过`。
覆盖核对（tests/test_line.py）：下->上判进（用例2）、上->下判出（用例3）、未穿线 0 事件（用例4：同侧移动+沿线平移）、重复计数防护（用例5：死区内抖动 0 事件，全程恰 1进1出），另含往复计数、未确认目标不计数、方向语义可翻转、垂直线、跳帧清空记忆、同帧双目标反向、reset、与 A5 确认器端到端联用，覆盖超出验收要求。

**3. GUI 验证——通过**
命令：`python src/app.py --video "C:/Users/OS/Desktop/farm_guard/videos/5G智慧农业素材.mp4" --max-frames 120 --dump-frame tests/a6_gui_frame_v.png`，退出码 0，日志 `带标注画面已保存：...tests\a6_gui_frame_v.png`。
ReadMediaFile 目检 dump 帧：画面中部渲染青色 WARNING LINE 警戒线，线中点有指向上方的 IN 方向箭头（与 下->上=进 语义一致），左上角青色底纹叠加 `IN: 0 OUT: 0` 双向计数。前 120 帧运行日志无任何穿线事件（仅有帧89/103 两条 A5 确认记录），GUI 计数 0/0 与日志一致。计数与告警联动代码路径 `src/app.py:639 _append_cross_events`（事件写入告警列表并附累计计数）审查无误。

**4. 回归——通过**
`python tests/test_confirm.py`：退出码 0，7 组用例全 PASS。
`python tests/test_zone.py`：退出码 0，4 组用例全 PASS。

**5. 合规——通过**
`reference/` 目录为空（仅 .gitkeep），逐文件比对 `src/` 8 个 .py 与 reference 无 ≥3 行以上的长行重合，无原文复制嫌疑；`src/line_counter.py` 为带完整中文 docstring 的自研实现。
交付目录无视频文件；项目根存在 `yolov8n.pt`（6.5MB，A1 起即在，detector 默认从根加载）——此为 plan.md A9 已列入的收尾迁移项（移入 `weights/`），非 A6 引入，沿用既往轮次口径仅作备注。
新增配置项核对（`src/config.py:65-79`）：`warning_line=(0.0,0.5,1.0,0.5)`、`line_enter_direction=True`、`line_count_enabled=True`、`line_deadzone_px=3`、`line_track_max_age=30`，均被 line_counter.py/app.py 真实消费，取值合理（死区 3px 防抖、轨迹侧记忆 30 帧遗忘）。

### 判定：PASS

## 第 2 次测试

### 判定：PASS
